{
  "name": "image-analysis-server",
  "version": "1.0.0",
  "private": true,
  "description": "Servidor HTTP local estático para o benchmark de formato/qualidade/resolução de imagens (TCC).",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "tsx src/server.ts",
    "build": "tsc --noEmit",
    "test": "node --import tsx --test test/**/*.test.ts"
  },
  "devDependencies": {
    "tsx": "4.23.1",
    "typescript": "5.9.3",
    "@types/node": "22.20.1"
  }
}
